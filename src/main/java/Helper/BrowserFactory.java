/**
 * 
 */
package Helper;

/**
 * @author pkoduru
 *it will accept the browser name as aparameter  respectiver browser will execute
 */
public class BrowserFactory {

	public void startBrowser(String browserName) {
		if(browserName.equalsIgnoreCase("ChromeBrowser")) {
			
		}
	}
	
	
}
